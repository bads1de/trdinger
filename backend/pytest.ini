[pytest]
# NOTE: Keep ASCII-only to avoid Windows cp932 decoding issues
testpaths = tests
pythonpath =
    .
addopts =
    -q
    --maxfail=1
    --disable-warnings
markers =
    slow: slow tests
    e2e: end-to-end tests
    performance: performance tests
    integration: integration tests